[project]
name = "star-research"
version = "0.1.0"
description = "Repositorio de materiais para alunos da STAR Research Institute"
requires-python = ">=3.11.3"
dependencies = [
    "numpy",
    "matplotlib",
    "jupyter",
]
